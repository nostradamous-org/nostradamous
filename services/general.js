function General(baseAxios) {
  return {
    // getCourseCategories() {
    //   let url = `${process.env.baseUrl}${process.env.baseApiUrl}/courses/categories/`
    //   return baseAxios.get(url)
    // },
  }
}

export { General }
